# Sprite2D MCP

Vue 3 精灵表编译工具（Windows 风格界面）。工程文档见 `ai-software-engineering/`。

```bash
npm install
npm run dev
```

构建：`npm run build`，输出 `dist/`。亦可用根目录 `build.py` / `run.py` / `test.py` / `publish.py` / `dev.py`。
