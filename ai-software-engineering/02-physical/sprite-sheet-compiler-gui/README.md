# sprite-sheet-compiler-gui

| 项 | 值 |
|----|-----|
| 产物 | 静态站点（`dist/`：`index.html` + assets） |
| 类型 | Web SPA |
| 源码根 | 仓库根目录 `src/` |
| 构建 | `npm run build`（Vite） |
| 对应逻辑 | `01-logic/` UI 壳 + 精灵管线 |

## 制品

- `spec.md`：行为与字段权威
- `mapping.md`：元素 → 源码路径
- `uml-*.md`：组件与结构示意
